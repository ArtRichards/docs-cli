# Deployment Rollout Plan

BODYMARKER — this file's H1 ends with the word "Plan", which `docs migrate`
would read as `Role: plan` via H1-trailing-word inference.

## Section

`docs stamp` does NO role inference: with no `--role`, this doc must get
the default `Role: notes`, never `Role: plan`.
