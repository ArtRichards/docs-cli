# A — recognized edge to a data file

Lifecycle: active
Role: spec
Project: reciprocal-nonmd
Updated: 2026-05-20

Related:
- depends-on: data.yaml

## Body

The convention deliberately allows a `Related:` target that is not a managed Markdown doc. `data.yaml` exists, so there is no `broken-ref`, and a YAML file cannot declare an inverse — no finding.
